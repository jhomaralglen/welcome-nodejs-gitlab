apiVersion: v1
kind: Service
metadata:
  name: welcome-nodejs-gitlab
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: http
    name: http
  selector:
    app: welcome-nodejs-gitlab
  type: ClusterIP